# AutoPickup configuration
# Messages support & color codes (e.g. &a for green)

settings:
  # Default state for new players who have never used /autopickup
  default-enabled: false

  # ActionBar pickup notification
  actionbar:
    # Enable or disable the actionbar pickup notification
    enabled: true
    # How many ticks the actionbar remains visible after the last pickup (20 ticks = 1 second)
    display-ticks: 40

messages:
  toggled-on: "&aAuto-pickup has been &fenabled&a."
  toggled-off: "&cAuto-pickup has been &fdisabled&c."
  no-permission: "&cYou do not have permission to use this command."
  players-only: "&cThis command can only be used by players."
  invalid-usage: "&cUsage: /autopickup [on|off|mode|reload]"
  reloaded: "&aConfiguration reloaded."
